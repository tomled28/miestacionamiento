function initMap(){
    var options = {
        zoom : 12,
        center : {lat: -31.3112115, lng: -64.283903215}    
    }
    var map = new google.maps.Map(document.getElementById('map'), options );
}
